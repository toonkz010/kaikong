<!DOCTYPE html>
<html lang="th">
<head>
<meta charset="UTF-8">
<title>ใบรับรองแทนใบเสร็จรับเงิน</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --ink:#1c2a4a;
    --line:#5b6b95;
    --paper:#dfe9e4;
    --paper-edge:#c9d8d1;
    --accent:#3d5a99;
    --accent-soft:#e7edf7;
  }
  *{box-sizing:border-box;}
  body{
    font-family:'Sarabun', sans-serif;
    background:#eef1f0;
    margin:0;
    padding:24px 12px 60px;
    color:var(--ink);
  }
  .toolbar{
    max-width:900px;
    margin:0 auto 16px;
    display:flex;
    flex-wrap:wrap;
    gap:10px;
    align-items:center;
  }
  .toolbar .label{
    font-weight:600;
    font-size:14px;
    margin-right:4px;
    color:#33415e;
  }
  .quick-btn{
    border:1.5px solid var(--accent);
    background:#fff;
    color:var(--accent);
    padding:8px 16px;
    border-radius:20px;
    font-family:inherit;
    font-size:14px;
    font-weight:600;
    cursor:pointer;
    transition:all .15s ease;
  }
  .quick-btn:hover{ background:var(--accent); color:#fff; }
  .quick-btn:active{ transform:scale(0.97); }
  .actions{
    max-width:900px;
    margin:0 auto 16px;
    display:flex;
    gap:10px;
    justify-content:flex-end;
  }
  .action-btn{
    border:none;
    padding:9px 18px;
    border-radius:6px;
    font-family:inherit;
    font-size:14px;
    font-weight:600;
    cursor:pointer;
  }
  .btn-print{ background:var(--accent); color:#fff; }
  .btn-clear{ background:#fff; color:#a33; border:1.5px solid #a33; }
  .btn-addrow{ background:#fff; color:var(--accent); border:1.5px solid var(--accent); }

  .sheet{
    max-width:900px;
    margin:0 auto;
    background:var(--paper);
    border:1px solid var(--paper-edge);
    padding:34px 38px 28px;
    box-shadow:0 4px 18px rgba(0,0,0,0.12);
  }
  .header{ text-align:center; margin-bottom:14px; }
  .h-title{ font-size:22px; font-weight:700; margin:0 0 2px; }
  .h-shop{ font-size:26px; font-weight:700; color:var(--accent); margin:2px 0 8px; }
  .h-shop-input{
    border:none; background:transparent; text-align:center;
    font-family:inherit; font-size:26px; font-weight:700; color:var(--accent);
    width:100%; outline:none;
  }
  .h-line{ font-size:14.5px; margin:3px 0; }
  .h-line input{
    border:none; background:transparent; font-family:inherit; font-size:14.5px;
    color:var(--ink); text-align:center; outline:none;
  }
  .buyer-row{
    display:flex; align-items:flex-end; gap:6px; margin:18px 0 10px;
    font-size:14.5px;
  }
  .buyer-row input{
    flex:1; border:none; border-bottom:1px dotted var(--line);
    background:transparent; font-family:inherit; font-size:14.5px;
    padding:2px 4px; outline:none;
  }

  table{
    width:100%; border-collapse:collapse; margin-top:8px;
    border:1.5px solid var(--line);
  }
  th{
    border:1px solid var(--line);
    background:var(--accent-soft);
    font-size:14px; font-weight:700;
    padding:8px 4px;
  }
  td{
    border:1px solid var(--line);
    padding:0;
    height:34px;
  }
  td input{
    width:100%; height:100%; border:none; background:transparent;
    font-family:inherit; font-size:14px; padding:4px 6px; outline:none;
    text-align:center;
  }
  td.desc input{ text-align:left; }
  td.amount input{ text-align:right; padding-right:10px; }
  col.date{ width:14%; }
  col.desc{ width:46%; }
  col.amount{ width:20%; }
  col.remark{ width:20%; }

  .custom-add{
    display:flex; align-items:center; gap:8px; flex:1; min-width:220px;
  }
  .custom-add input{
    flex:1; border:1.5px solid var(--line); border-radius:20px;
    padding:7px 14px; font-family:inherit; font-size:14px; outline:none;
    min-width:120px;
  }

  .avg-row{
    display:flex; flex-wrap:wrap; justify-content:flex-end; align-items:center;
    gap:10px; margin-top:14px; font-size:14px;
  }
  .avg-row .lbl{ font-weight:600; }
  .avg-row input{
    border:1.5px solid var(--line); border-radius:6px; background:#fff;
    padding:6px 10px; font-family:inherit; font-size:14px;
    width:130px; text-align:right; outline:none;
  }

  .total-row{
    display:flex; justify-content:flex-end; align-items:center; gap:10px;
    margin-top:10px;
  }
  .total-row .lbl{ font-weight:700; font-size:15px; }
  .total-box{
    border:1.5px solid var(--line); background:#fff;
    padding:6px 14px; font-weight:700; font-size:15px; min-width:130px;
    text-align:right;
  }

  .cert{ margin-top:22px; font-size:14.5px; line-height:2.1; }
  .cert .fill{
    border:none; border-bottom:1px dotted var(--line); background:transparent;
    font-family:inherit; font-size:14.5px; outline:none; padding:0 4px;
  }
  .sign-block{ margin-top:26px; font-size:14.5px; line-height:2.4; }
  .sign-block .fill{ width:260px; }

  @media print{
    body{ background:#fff; padding:0; }
    .toolbar, .actions{ display:none; }
    .sheet{ box-shadow:none; border:none; max-width:100%; }
  }
  @media (max-width:600px){
    .sheet{ padding:20px 14px; }
    .h-title{ font-size:18px; }
    .h-shop-input{ font-size:20px; }
  }
</style>
</head>
<body>

<div class="toolbar">
  <span class="label">เพิ่มรายการลัด:</span>
  <button class="quick-btn" onclick="quickAdd('ของสด')">ของสด</button>
  <button class="quick-btn" onclick="quickAdd('หมูสดไก่สด')">หมูสดไก่สด</button>
  <button class="quick-btn" onclick="quickAdd('กับข้าว')">กับข้าว</button>
  <div class="custom-add">
    <input type="text" id="customItem" placeholder="พิมพ์รายการเอง..." onkeydown="if(event.key==='Enter'){event.preventDefault(); quickAddCustom();}">
    <button class="quick-btn" onclick="quickAddCustom()">+ เพิ่ม</button>
  </div>
</div>

<div class="actions">
  <button class="action-btn btn-addrow" onclick="addRow()">+ เพิ่มแถว</button>
  <button class="action-btn btn-clear" onclick="clearForm()">ล้างข้อมูล</button>
  <button class="action-btn btn-print" onclick="window.print()">พิมพ์ / บันทึกเป็น PDF</button>
</div>

<div class="sheet">
  <div class="header">
    <p class="h-title">ใบรับรองแทนใบเสร็จรับเงิน</p>
    <input class="h-shop-input" value="ร้านแอลเอสพาณิชย์">
    <p class="h-line">
      <input style="width:20px" value="24"> หมู่ที่ <input style="width:20px" value="7">
      ตำบล<input style="width:110px" value="บ้านเขว้า">
      อำเภอ<input style="width:110px" value="บ้านเขว้า">
      จังหวัด<input style="width:90px" value="ชัยภูมิ">
      <input style="width:60px" value="36170">
    </p>
    <p class="h-line">โทร. <input style="width:130px" value="065-9321492"></p>
    <p class="h-line">เลขประจำตัวผู้เสียภาษีอากร <input style="width:150px" value="1369900570575"></p>
  </div>

  <div class="buyer-row">
    <input placeholder="ชื่อผู้ซื้อ/หน่วยงาน">
    <span style="white-space:nowrap;">ผู้ซื้อ/ผู้รับบริการ</span>
  </div>

  <table>
    <colgroup>
      <col class="date"><col class="desc"><col class="amount"><col class="remark">
    </colgroup>
    <thead>
      <tr>
        <th>วัน/เดือน/ปี</th>
        <th>รายละเอียดรายจ่าย</th>
        <th>จำนวนเงิน</th>
        <th>หมายเหตุ</th>
      </tr>
    </thead>
    <tbody id="rows"></tbody>
  </table>

  <div class="avg-row">
    <span class="lbl">ยอดที่ต้องการเฉลี่ย:</span>
    <input type="number" step="0.01" id="avgTarget" placeholder="0.00">
    <button class="quick-btn" onclick="distributeAverage()">แบ่งยอดแบบสุ่ม</button>
  </div>

  <div class="total-row">
    <span class="lbl">รวมทั้งสิ้น</span>
    <span class="total-box" id="totalBox">0.00</span>
  </div>

  <div class="cert">
    ข้าพเจ้า <input class="fill" style="width:220px"> (ผู้เบิกจ่าย) ตำแหน่ง <input class="fill" style="width:180px"><br>
    ขอรับรองว่า รายจ่ายข้างต้นนี้ไม่อาจเรียกเก็บใบเสร็จรับเงินจากผู้รับได้ และข้าพเจ้าได้จ่ายไปในงาน<br>
    ของทาง บริษัท/ห้างหุ้นส่วนจำกัด <input class="fill" style="width:200px">
    โดยตั้งแต่วันที่ <input class="fill" style="width:110px"> ถึงวันที่ <input class="fill" style="width:110px">
  </div>

  <div class="sign-block">
    ลงชื่อ <input class="fill" style="width:260px"> (ผู้เบิกจ่าย)<br>
    ลงชื่อ <input class="fill" style="width:260px"> (ผู้อนุมัติ)
  </div>
</div>

<script>
const ROW_COUNT_DEFAULT = 10;
const tbody = document.getElementById('rows');
const totalBox = document.getElementById('totalBox');

function todayThai(){
  try{
    return new Date().toLocaleDateString('th-TH-u-ca-buddhist', {day:'2-digit', month:'2-digit', year:'numeric'});
  }catch(e){
    return new Date().toLocaleDateString('th-TH');
  }
}

function makeRow(){
  const tr = document.createElement('tr');
  tr.innerHTML = `
    <td><input type="text" class="date-input" value="${todayThai()}"></td>
    <td class="desc"><input type="text" placeholder=""></td>
    <td class="amount"><input type="number" step="0.01" placeholder="" oninput="updateTotal()"></td>
    <td><input type="text" placeholder=""></td>
  `;
  tbody.appendChild(tr);
}

function addRow(){ makeRow(); }

function init(){
  for(let i=0;i<ROW_COUNT_DEFAULT;i++) makeRow();
}
init();

function insertItem(label){
  const descInputs = tbody.querySelectorAll('td.desc input');
  let target = null;
  descInputs.forEach(inp=>{ if(!target && inp.value.trim()==='') target = inp; });
  if(!target){
    makeRow();
    const all = tbody.querySelectorAll('td.desc input');
    target = all[all.length-1];
  }
  target.value = label;
  const row = target.closest('tr');
  row.querySelector('td.amount input').focus();
}

function quickAdd(label){
  insertItem(label);
}

function quickAddCustom(){
  const box = document.getElementById('customItem');
  const val = box.value.trim();
  if(val === '') { box.focus(); return; }
  insertItem(val);
  box.value = '';
}

function updateTotal(){
  const amounts = tbody.querySelectorAll('td.amount input');
  let sum = 0;
  amounts.forEach(inp=>{
    const v = parseFloat(inp.value);
    if(!isNaN(v)) sum += v;
  });
  totalBox.textContent = sum.toLocaleString('th-TH', {minimumFractionDigits:2, maximumFractionDigits:2});
}

function distributeAverage(){
  const targetInput = document.getElementById('avgTarget');
  const target = parseFloat(targetInput.value);
  if(isNaN(target) || target <= 0){
    alert('กรุณาใส่ยอดที่ต้องการเฉลี่ยก่อน');
    targetInput.focus();
    return;
  }
  const rows = Array.from(tbody.querySelectorAll('tr')).filter(tr=>{
    const desc = tr.querySelector('td.desc input').value.trim();
    return desc !== '';
  });
  const n = rows.length;
  if(n === 0){
    alert('ยังไม่มีรายการให้เฉลี่ย กรุณาเพิ่มรายการก่อน');
    return;
  }

  // generate n random positive weights, normalize so they sum to 1,
  // clamp so no single share is too extreme (keeps values reasonable)
  let weights = [];
  for(let i=0;i<n;i++) weights.push(0.5 + Math.random()); // range 0.5–1.5
  const weightSum = weights.reduce((a,b)=>a+b, 0);

  // round each share to nearest 1 baht (or 0.25 for smaller totals), then
  // fix rounding drift on the last row so the total matches exactly
  const roundTo = target >= 200 ? 1 : (target >= 50 ? 5/10 : 0.25);
  let amounts = weights.map(w => Math.round((target * w / weightSum) / roundTo) * roundTo);

  // fix drift so amounts sum exactly to target
  let drift = Math.round((target - amounts.reduce((a,b)=>a+b,0)) * 100) / 100;
  amounts[amounts.length - 1] = Math.round((amounts[amounts.length - 1] + drift) * 100) / 100;

  // guard against a negative last value from a large drift; if it happens,
  // spread the drift across all rows instead
  if(amounts[amounts.length - 1] < 0){
    const even = Math.round((target / n) * 100) / 100;
    amounts = new Array(n).fill(even);
    const evenDrift = Math.round((target - amounts.reduce((a,b)=>a+b,0)) * 100) / 100;
    amounts[n-1] = Math.round((amounts[n-1] + evenDrift) * 100) / 100;
  }

  rows.forEach((tr, idx)=>{
    tr.querySelector('td.amount input').value = amounts[idx].toFixed(2);
  });
  updateTotal();
}

function clearForm(){
  if(!confirm('ต้องการล้างข้อมูลทั้งหมดในฟอร์มหรือไม่?')) return;
  tbody.innerHTML = '';
  init();
  updateTotal();
  document.getElementById('avgTarget').value = '';
  document.querySelectorAll('.cert input, .sign-block input, .buyer-row input').forEach(i=>i.value='');
}
</script>
</body>
</html>
